package Notes;

// Notes.Notes.Rs200Note class to represent a 200 Rs note
public class Rs200Note extends NotesAction.Note { // Declaring Notes.Notes.Rs200Note class that extends Note

    // Constructor for Notes.Notes.Rs200Note
    public Rs200Note() {

        super(200); // Calling constructor of superclass Note with value 200
    }
}
