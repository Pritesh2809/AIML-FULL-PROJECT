// Rs200Note class
public class Rs200Note extends NotesAction.Note { // Declaring Rs200Note class that extends Note
    public Rs200Note() { // Constructor for Rs200Note
        super(200); // Calling constructor of superclass Note with value 200
    }
}

